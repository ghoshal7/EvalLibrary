# extraction_eval Evaluation Skill (pattern: extraction)

TODO(when-to-use): one paragraph describing the workloads this skill evaluates.

## Dataset schema
TODO: document required `inputs` and `expected` fields per case.

## Procedure
1. `python run.py --dataset <path> --target <module:function|trace_dir>`
2. TODO(interpretation): what a failing metric means and what to inspect.

## Customization
Never edit in place for one app — use the customize_skill meta-skill.
